Work is divided between the Node and Lexer class.

In the Node class, Create (kind.equals("BIF")) function for each new BIF
Call Java's Math class to return answer for the BIF
(Check line 211 for reference)

In the Lexer class, In state 2, add more else if (data.equals("...")) for the BIF
(Starts around line 156)

*Be sure to look at "corgiCFG.txt" for more explanation on the BIF

Sarah:
lt( , ), le( , ), eq( , ), ne( , )

Sara:
or( , ), and( , ), not( ), print(<expr>)

Ze:
nl(), round(<expr>), trunc(<expr>)

-- 
Eric: Work on Parser class